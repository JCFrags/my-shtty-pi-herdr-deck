import assert from "node:assert/strict";
import { createServer, type Socket } from "node:net";
import { chmod, mkdtemp, unlink, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import test from "node:test";
import { NdjsonDecoder, encodeFrame } from "../../src/shared/protocol/codec.js";
import { createFakePiHarness, waitFor } from "../helpers.js";

function apiFor(harness: ReturnType<typeof createFakePiHarness>) { const handlers = new Map<string, Array<(event: unknown, context: unknown) => void | Promise<void>>>(); const tools: unknown[] = []; const api = { ...harness.pi, on: (name: string, handler: (event: unknown, context: unknown) => void | Promise<void>) => { handlers.set(name, [...(handlers.get(name) ?? []), handler]); }, registerCommand: () => undefined, registerTool: (tool: unknown) => { tools.push(tool); } }; return { api, handlers, tools }; }
async function emit(h: ReturnType<typeof apiFor>, name: string, context: unknown, event: unknown = {}): Promise<void> { for (const handler of h.handlers.get(name) ?? []) await handler(event, context); }

test("orchestrator extension reload transfers runtime credential and registers tools once", async () => { const root = await mkdtemp(join(tmpdir(), "pi-orch-extension-")); const socketPath = join(root, "broker.sock"); const tokenPath = join(root, "token"); await writeFile(tokenPath, "managed-token-value-abcdefghijklmnopqrstuvwxyz\n", { mode: 0o600 }); await chmod(tokenPath, 0o600); const connections: Socket[] = []; let registrationCount = 0; const server = createServer((socket) => { connections.push(socket); const decoder = new NdjsonDecoder<unknown>((value) => value); socket.on("data", (data) => { for (const item of decoder.push(data)) { if (!item.ok || !item.value || typeof item.value !== "object") continue; const frame = item.value as Record<string, unknown>; if (frame.type === "hello") socket.write(encodeFrame({ v: 1, type: "hello_result", id: frame.id, ok: true, broker: { version: "test", status: "healthy", lastEventSeq: 1 }, principal: { id: "prn_child", kind: "pi_child", permissions: ["read:state"] }, limits: { maxLineBytes: 1_048_576 } })); else if (frame.type === "request") { if (frame.method === "agent.register_managed") registrationCount++; socket.write(encodeFrame({ v: 1, type: "response", id: frame.id, method: frame.method, ok: true, result: frame.method === "agent.register_managed" ? { agentId: "agt_child", generation: 1, connectionGeneration: registrationCount, heartbeatMs: 5000, permissions: ["read:state"] } : {} })); } } }); }); await new Promise<void>((resolve) => server.listen(socketPath, resolve)); const previous = { managed: process.env.PI_HERDR_ORCH_MANAGED, pane: process.env.HERDR_PANE_ID, bin: process.env.HERDR_BIN_PATH, socket: process.env.PI_HERDR_ORCH_BROKER_SOCKET, session: process.env.PI_HERDR_ORCH_SESSION_KEY, token: process.env.PI_HERDR_ORCH_TOKEN_FILE, agent: process.env.PI_HERDR_ORCH_AGENT_ID, generation: process.env.PI_HERDR_ORCH_GENERATION }; Object.assign(process.env, { PI_HERDR_ORCH_MANAGED: "1", HERDR_PANE_ID: "pane", HERDR_BIN_PATH: "/bin/true", PI_HERDR_ORCH_BROKER_SOCKET: socketPath, PI_HERDR_ORCH_SESSION_KEY: "session-key", PI_HERDR_ORCH_TOKEN_FILE: tokenPath, PI_HERDR_ORCH_AGENT_ID: "agt_child", PI_HERDR_ORCH_GENERATION: "1" }); try { const extension = (await import(`../../extensions/pi-herdr-orchestrator.js?harness=${Date.now()}`)).default; const firstHarness = createFakePiHarness(); const first = apiFor(firstHarness); await extension(first.api as never); await emit(first, "session_start", firstHarness.context); await waitFor(() => registrationCount === 1); const firstShutdown = first.handlers.get("session_shutdown") ?? []; for (const handler of firstShutdown) await handler({ reason: "reload" }, firstHarness.context); await unlink(tokenPath); const secondHarness = createFakePiHarness(); const second = apiFor(secondHarness); await extension(second.api as never); await emit(second, "session_start", secondHarness.context); await waitFor(() => registrationCount === 2); assert.equal(first.tools.length, 2); assert.equal(second.tools.length, 2); assert.equal(new Set((second.tools as Array<{ name: string }>).map((tool) => tool.name)).size, 2); assert.equal(connections[0]?.destroyed, true); const secondShutdown = second.handlers.get("session_shutdown") ?? []; for (const handler of secondShutdown) await handler({ reason: "quit" }, secondHarness.context); assert.equal((globalThis as Record<PropertyKey, unknown>)[Symbol.for("pi-herdr-orchestrator.credential.v1")], undefined); } finally { for (const socket of connections) socket.destroy(); await new Promise<void>((resolve) => server.close(() => resolve())); for (const [key, value] of Object.entries(previous)) { const envKey = key === "managed" ? "PI_HERDR_ORCH_MANAGED" : key === "pane" ? "HERDR_PANE_ID" : key === "bin" ? "HERDR_BIN_PATH" : key === "socket" ? "PI_HERDR_ORCH_BROKER_SOCKET" : key === "session" ? "PI_HERDR_ORCH_SESSION_KEY" : key === "token" ? "PI_HERDR_ORCH_TOKEN_FILE" : key === "agent" ? "PI_HERDR_ORCH_AGENT_ID" : "PI_HERDR_ORCH_GENERATION"; if (value === undefined) delete process.env[envKey]; else process.env[envKey] = value; } } });
