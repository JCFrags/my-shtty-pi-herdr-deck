{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Herdr API request schema (v0.8.0 test projection)",
  "oneOf": [
    { "properties": { "method": { "const": "agent.list" } } },
    { "properties": { "method": { "const": "agent.focus" } } },
    { "properties": { "method": { "const": "plugin.pane.focus" } } },
    { "properties": { "method": { "const": "plugin.pane.close" } } }
  ]
}
